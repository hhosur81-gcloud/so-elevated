# Use FastAPI Mock Services for Enterprise Backends

To enable deterministic local testing, validation, and demoing without dependencies on live third-party enterprise sandboxes, we implement embedded FastAPI mock services for WorkWeek (HCM) and ServiceImmediately (ITSM) with seeded stateful JSON fixtures and RESTful API endpoints.
